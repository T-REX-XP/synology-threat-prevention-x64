# Unused experimental SPA

This webpack UI is **not packed** and is not the Threat Prevention desktop app.

The shipped window is official ExtJS (`synoips.js`) plus
`spk/src/threatprevention/package/ui/bridge/` inlined at pack time.

Do not restore this SPA as the product UI.
